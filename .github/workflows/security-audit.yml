name: Security Audit

on:
  push:
    branches:
      - main

jobs:

  audit:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Run npm audit
        run: |
          npm audit || true
